---
uid: "112792"
seealso:
- linkId: "112825"
- linkId: DevExpress.ExpressApp.Utils.ImageLoader
- linkId: DevExpress.Persistent.Base.ImageNameAttribute
- linkId: DevExpress.Persistent.Base.ImageSizeMode
- linkId: DevExpress.ExpressApp.Editors.StaticImage
- linkId: "113156"
- linkId: "113215"
title: Add and Replace Icons
---
# Add and Replace Icons

XAF applications can display icons next to [Actions](xref:112622), navigation items, [Detail View](xref:112611) captions, and business class names. You can add custom icons to your application, replace built-in XAF icons, and configure which UI elements display icons.

This help topic shows you how to:
- Add custom icons to your XAF application
- Replace built-in icons with custom alternatives
- Set icons for business classes, actions, and navigation items
- Configure image file formats and naming conventions

## Understanding Image Sources in XAF

XAF uses image sources to locate and load application icons. Understanding how image sources work helps you control which icons appear in the UI and troubleshoot display issues.

### Types of Image Sources

| Image Source | Description | Requirements |
|-|-|-|
| File Image Source | References icons stored in a folder on disk. Images are copied to the output directory at build time. | Set each image file's **Copy to Output Directory** to **Copy always**. |
| **DevExpress.Images.v<:xx.x:>** Assembly Resource Image Source | Contains all built-in XAF icons. Default image source. | None. |

### View Available Image Sources in Your Application

To view the available image sources in your application:

1. Open the [Model Editor](xref:112582) for your application project (for example, **MySolution.Win** or **MySolution.Blazor.Server**).
2. Navigate to the **ImageSources** node.
3. Review child nodes. Each node corresponds to an image source.

	![XAF Image Sources in Model Editor (ImageSources Node), DevExpress](~/images/addandoverrideimages115564.png)

A typical XAF application includes the following autogenerated image sources:

- **MySolution.Module** — Assembly Resource Image Source for your module's **Images** folder where you can add custom icons
- **DevExpress.Images.v<:xx.x:>** — The built-in DevExpress image library

## Image File Requirements

Before adding icons to your application, ensure your image files meet the following requirements:

### Supported Image File Formats

XAF applications support both SVG and raster images (PNG, JPG, BMP). The recommended format is SVG. If you have to use raster images, we recommend PNG.

Use SVG images for better display on High DPI screens.

### Image File Embedding

Embed custom images into assemblies as resources rather than using file references.

### Image File Names

When using image files, keep file names under 260 characters. This is important for [ClickOnce deployment](xref:113234), which can create long installation paths. If an image path exceeds 260 characters, the image is unavailable, and the issue is logged in the [Log File](xref:112575).

### Image Size Guidelines (Windows Forms)

**Raster Image Size**

| Image size | Image suffix |Description |
|---|---|---|
| **16x16** | none | Standard-sized images used for [Business Classes](xref:112570) |
| **32x32** | "32x32" | Used as Large Icons in the [Navigation Bar](xref:113198) and [Ribbon UI](xref:404212) |
| **48x48** | "48x48" | Used by the [Validation Module](xref:113684) |

**SVG Image Size**

An SVG image size is determined by a control or set in the Model Editor.

### Image Naming Conventions (Windows Forms)

Follow guidelines below when you name image files:

| Image | Name Description |
|---|---|
| _Employee.png_ | Do not include a size suffix for 16x16 raster images. |
| _Employee_32x32.png_ | Add a size suffix to 12x12, 24x24, 32x32 and 48x48 raster images. |
| _Office2013\Employee&#65279;.&#65279;png_ | Raster images are part of the **DevExpress.Images**'s **Colored** collection. To use images from another collection, add its name without spaces to an image name as prefix. You can see the available collections in the **Common PNG Images** tab of the [Image Picker](xref:113330#image-picker) dialog window. |
| _Employee.svg_ | SVG images. |

For business classes, use two PNG images (_ImageName.png_ and _ImageName\_32x32.png_) or one SVG image (_ImageName.svg_) for vector graphics. [Template Kit](xref:405447) enables SVG images for all new XAF WinForms applications. To use PNG images instead, set the [UseSvgImages](xref:DevExpress.ExpressApp.Utils.ImageLoader.UseSvgImages) property to `false.`

## Icon Types in ASP.NET Core Blazor

Two types of DevExpress themes are available for ASP.NET Core Blazor applications: [Fluent and Classic](xref:403068#devexpress-themes). XAF ships icon sets for both theme types and automatically switches icons based on the theme. You can use the @DevExpress.ExpressApp.Blazor.BlazorApplication.IconType property to select the icon set. Available options include:

[!include[blazor-icon-type](~/templates/blazor-icon-type.md)]

## Add Custom Icons

To add custom icons to your XAF application, create an **Images** folder in your module project and add your image files as embedded resources.

### Add Icons to a Module Project

1. In your module project (for example, **SolutionName.Module**), create an **Images** folder if it does not exist.

2. Add your image files to the **Images** folder.

3. For each image file, set the **Build Action** property to **Embedded Resource** in the file's properties.

The framework automatically creates an Assembly Resource Image Source for the **Images** folder in your module. Icons from this folder are available throughout your application for use with business classes, Actions, and UI elements.

### Add Icons from External Assemblies

If you have icons stored in a separate assembly:

1. Open the [Model Editor](xref:112582) in your module project.

2. Navigate to the **ImageSources** node.

3. Add a new **AssemblyResourceImageSource** node.

4. Set the **Assembly** property to the name of your image assembly.

5. Set the **Folder** property to the folder name containing the images.

6. Add a reference to the image assembly in all your module and application projects.

## Replace Built-in Icons

XAF includes default icons for common UI elements. To replace a built-in icon with your custom version, add an image with the same name to your module's **Images** folder.

1. In your module project (for example, **SolutionName.Module**), create an **Images** folder if it does not exist.

2. Add your custom image file to the **Images** folder. Name it exactly the same as the built-in icon you want to replace.

3. Set the **Build Action** property to **Embedded Resource** for the image file.

The framework searches for icons in the order specified by the **Index** property of image sources. If your module's image source has a lower index than the source containing the original icon, your custom icon will be used instead.

### Adjust Image Source Priority

To ensure your custom icons take precedence over built-in icons:

1. Open the [Model Editor](xref:112830) for your application project (for example, **SolutionName.Win** or **SolutionName.Blazor.Server**).

2. Navigate to the **ImageSources** node.

3. Locate the child nodes that correspond to different image sources.

4. Set the **Index** property for each image source node. Lower index values have higher priority.

> [!NOTE]
> Embedding images as resources is more reliable than using file-based image sources. While you can add images to an application project's **Images** folder, we recommend that you use embedded resources in module projects.

## Set Icons for UI Elements

After adding icons to your application, assign them to business classes, actions, and other UI elements.

### Enable Icons in UI

To display icons in the [Navigation System](xref:113198) and Tabbed MDI tabs, configure these Application Model properties:

| Property | Value | UI Item |
|-|-|-|
| [IModelRootNavigationItems.ShowImages](xref:DevExpress.ExpressApp.SystemModule.IModelRootNavigationItems.ShowImages) | `true` | Navigation items |
| [IModelOptionsWin.ShowTabImage](xref:DevExpress.ExpressApp.Win.SystemModule.IModelOptionsWin.ShowTabImage) | `true` | Tab images (WinForms) |
| [IModelOptionsBlazor.ShowTabImage](xref:DevExpress.ExpressApp.Blazor.SystemModule.IModelOptionsBlazor.ShowTabImage) | `true` | Tab images (ASP.NET Core Blazor) |
| @DevExpress.ExpressApp.Model.IModelLayoutManagerOptions.EnableLayoutGroupImages | `true` | [Tabbed layout groups](xref:2170) |

### Set Icons in the Application Model

The [Application Model](xref:112580) has an **ImageName** property for UI elements that can display icons. For example, the **BOModel** | **_\<Class\>_** node's **ImageName** property controls the icon shown for a business class in Detail Views and other UI elements.

[!include[Naming Rules for ImageName Property Value](~/templates/imagename-naming-rules.md)]

To set an icon using the Model Editor:

1. Open the Model Editor and navigate to the element you want to configure.

2. Locate the **ImageName** property.

3. Click the ellipsis button to open the **Image Picker** dialog.

4. Browse and select the icon you want to use.

    ![ModelEditor Image Picker](~/images/model-editor-image-picker.png)

	> [!NOTE]
	> The **Image Picker** may not display images from a custom image source if an **Assembly Resource Image Source** was added in the same model differences file. You can type the **ImageName** property value manually.

### Set Icons in Code

You can assign icons programmatically instead of using the Model Editor.

[!include[Naming Rules for ImageName Property Value](~/templates/imagename-naming-rules.md)]

#### Set an Icon for a Business Class

Apply the [ImageNameAttribute](xref:DevExpress.Persistent.Base.ImageNameAttribute) to your business class:

**File:** _SolutionName.Module/BusinessObjects/Department.cs_

# [C#](#tab/tabid-csharp)

```csharp
using DevExpress.Persistent.Base;
using DevExpress.Persistent.BaseImpl;
using DevExpress.Xpo;

namespace SolutionName.Module.BusinessObjects {
    [ImageName("Department")]
    public class Department : BaseObject {
        public Department(Session session) : base(session) { }
        //...
    }
}
```
***

#### Set an Icon for an Action

Use the [ActionBase.ImageName](xref:DevExpress.ExpressApp.Actions.ActionBase.ImageName) property in your controller:

**File:** _SolutionName.Module/Controllers/MyController.cs_

# [C#](#tab/tabid-csharp)

```csharp
using DevExpress.ExpressApp;
using DevExpress.ExpressApp.Actions;

namespace SolutionName.Module.Controllers {
    public class MyController : ViewController {
        public MyController() {
            var myAction = new SimpleAction(this, "MyAction", PredefinedCategory.View) {
                Caption = "My Action",
                ImageName = "Action_Refresh"
            };
        }
    }
}
```
***
