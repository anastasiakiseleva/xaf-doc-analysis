---
uid: "404519"
title: 'XAF0031: Add the UseChangeTrackingProxies() call for automatic UI updates'
owner: Anastasiya Kisialeva
---
# XAF0031: Add the UseChangeTrackingProxies() call for automatic UI updates

**Severity: Warning**

Enable the `DbContextOptionsBuilder.UseChangeTrackingProxies()` option for automatic UI updates and change notifications from business object setters.

For more information, refer to the following topic: [PropertyChanged and CollectionChanged Event in Entity Framework Core](xref:117395#propertychanged-and-collectionchanged-event-in-entity-framework-core).

## Examples

See the _Startup.cs_ file in **YourSolutionName.Blazor.Server** and **YourSolutionName.Win** projects:

### Invalid Code

# [ASP.NET Core Blazor](#tab/tabid-blazor)
```csharp
public void ConfigureServices(IServiceCollection services) {
    services.AddXaf(Configuration, builder => {
        // ...
        builder.ObjectSpaceProviders
            .AddEFCore(options => options.PreFetchReferenceProperties())
                .WithDbContext<YourSolutionName.Module.BusinessObjects.YourSolutionNameEFCoreDbContext>((serviceProvider, options) => {
                    // ...
                    options.UseSqlServer(connectionString);
                    options.UseObjectSpaceLinkProxies();
                    options.UseLazyLoadingProxies();
                })
            .AddNonPersistent();
    });
}
```
# [Windows Forms](#tab/tabid-winforms)
```csharp
public static WinApplication BuildApplication(string connectionString) {
    // ...
    builder.ObjectSpaceProviders
        .AddEFCore(options => options.PreFetchReferenceProperties())
            .WithDbContext<YourSolutionName.Module.BusinessObjects.YourSolutionNameEFCoreDbContext>((application, options) => {                    
                options.UseSqlServer(connectionString);
                options.UseObjectSpaceLinkProxies();
            })
        .AddNonPersistent();
    // ...
}
```
***

### Valid Code

# [ASP.NET Core Blazor](#tab/tabid-blazor)
```csharp{9}
public void ConfigureServices(IServiceCollection services) {
    services.AddXaf(Configuration, builder => {
        // ...
        builder.ObjectSpaceProviders
            .AddEFCore(options => options.PreFetchReferenceProperties())
                .WithDbContext<YourSolutionName.Module.BusinessObjects.YourSolutionNameEFCoreDbContext>((serviceProvider, options) => {
                    // ...
                    options.UseSqlServer(connectionString);
                    options.UseChangeTrackingProxies();
                    options.UseObjectSpaceLinkProxies();
                    options.UseLazyLoadingProxies();
                })
            .AddNonPersistent();
    });
}
```

# [Windows Forms](#tab/tabid-winforms)
```csharp{7}
public static WinApplication BuildApplication(string connectionString) {
    // ...
    builder.ObjectSpaceProviders
        .AddEFCore(options => options.PreFetchReferenceProperties())
            .WithDbContext<YourSolutionName.Module.BusinessObjects.YourSolutionNameEFCoreDbContext>((application, options) => {                    
                options.UseSqlServer(connectionString);
                options.UseChangeTrackingProxies();
                options.UseObjectSpaceLinkProxies();
            })
        .AddNonPersistent();
    // ...
}
```
***
