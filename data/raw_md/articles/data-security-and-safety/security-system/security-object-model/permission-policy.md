---
uid: "116172"
seealso:
- linkId: "116170"
- linkId: "113556"
- linkId: "400290"
- linkId: "118740"
title: Roles and Permission Policy
---
# Roles and Permission Policy

The **Allow/Deny** Permission Policy determines Security System's behavior when there are no explicitly specified permissions for a specific type, object, or member.

The application's administrators can allow access to all data within the application for a specific role and simultaneously deny access to certain data types or members. They can also deny access to all data for a role and only allow access to a specific list of objects or members.

> [!NOTE]
> You should [upgrade an existing project to the Allow/Deny Permissions Policy](https://supportcenter.devexpress.com/ticket/details/t418166/how-to-upgrade-an-existing-project-to-the-allow-deny-permission-policy-migrate-to) if you created an application in XAF v16.1 or earlier. If you use the Entity Framework as the ORM system, you may also need to [perform a migration](https://supportcenter.devexpress.com/ticket/details/t459507/obsolete-how-to-add-navigation-permissions-to-an-entity-framework-6-ef-6-application) to switch from the obsolete **Deny** to the **Allow/Deny** policy.

The Permission Policy uses the following security user and role types:

| &nbsp; | Built-in XPO classes | Built-in EF Core classes | Common interfaces to support in custom classes |
|---|---|---|---|
| **User&nbsp;Type** | [](xref:DevExpress.Persistent.BaseImpl.PermissionPolicy.PermissionPolicyUser) | [](xref:DevExpress.Persistent.BaseImpl.EF.PermissionPolicy.PermissionPolicyUser) | [](xref:DevExpress.Persistent.Base.IPermissionPolicyUser) |
| **Role&nbsp;Type** | [](xref:DevExpress.Persistent.BaseImpl.PermissionPolicy.PermissionPolicyRole) | [](xref:DevExpress.Persistent.BaseImpl.EF.PermissionPolicy.PermissionPolicyRole) | [](xref:DevExpress.Persistent.Base.IPermissionPolicyRole) |

Note that applications generated by [Template Kit](xref:405447) use the `ApplicationUser` class (found in _SolutionName.Module\BusinessObjects\ApplicationUser.cs_) to store user data. This class extends [](xref:DevExpress.Persistent.BaseImpl.PermissionPolicy.PermissionPolicyUser) and implements the @DevExpress.ExpressApp.Security.ISecurityUserWithLoginInfo interface required to store user login information in applications with multiple authentication methods.

The **PermissionPolicyRole** classes expose the [IPermissionPolicyRole.PermissionPolicy](xref:DevExpress.Persistent.Base.IPermissionPolicyRole.PermissionPolicy) property:

![PermissionPolicy](~/images/permissionpolicy123615.png)

You can use this property to assign "deny all", "read-only all", or "allow all" default Permission Policies to each role, and explicitly specify the _Allow_ or _Deny_ modifier or leave it blank for each operation.

The Security System checks whether access to an object's property is allowed on several levels. The following image shows the levels in priority order:

![PermissionsPriority](~/images/permissionspriority123616.png)

"Member Permission by Criteria" has the highest priority and the Security System processes it first. The Security System uses explicit permissions at this level to determine whether access is allowed. If the role does not have any explicit permissions on this level, the Security System processes permissions at the next level according to the scheme above.

The "Role's Permission Policy" has the lowest priority and is used only when [type, object, and member permissions](xref:113366) are not explicitly specified.
