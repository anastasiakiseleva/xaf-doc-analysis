> [!Tip]
> Use these naming rules:
> - For images in the root of the **Images** folder: `"ImageName"` (for example, `"Department"`)
> - For images in a subfolder: `"SubfolderName.ImageName"` (for example, `"MyBusinessClasses.Department"`)