> [!NOTE]
> If you apply the [AutoExpand](https://learn.microsoft.com/en-us/odata/webapi/autoexpand-attribute) attribute to a referenced property, you do not need to explicitly specify the `$expand` parameter in an OData query because it is automatically loaded and its data is included in the API response.
